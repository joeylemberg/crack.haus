define(function (require) {
    // Load any app-specific modules
    // with a relative require call,
    // like:
   /* window.Main = require('./main');
    window.History = require('./history');
    window.Profile = require('./profile');
    window.Arcade = require('./arcade');
    window.Match = require('./match');
    window.Game = require('./game');
    window.Api = require('./api');
    window.Util = require('./util');
    window.Notification = require('./notification');
    window.User = require('./user');

    // Load library/vendor modules using
    // full IDs, like:
 //   var print = require('print');
    $(document).ready(function(){
		User.init();
		History.init();
	});*/
    //print(messages.getHello());
});
